/* Given a string, create a new string made up of its last two letters, reversed and separated by a space */

function lastLetters(word){
    var invert=word[word.length-1]+" "+word[word.length.2];
    return invert;
}
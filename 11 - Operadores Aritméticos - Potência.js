'use strict';

/* Na linha 7, declare uma variável chamada 'potencia' e atribua a ela o valor da variável 'base' elevada a variável 'expoente' */

function exponencial(base, expoente) {

var potencia = base**expoente;

return potencia;
}